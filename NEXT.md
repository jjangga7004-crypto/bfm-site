# NEXT — 아침에 읽을 요약 (2026-09-01 자율 세션 + 알고리즘 강화, 브랜치 `batch8-accuracy`)

> 대표 지시(같은 날 저녁): "라이브 디자인을 모조리 바꿀 것이므로 그 전에 엔진을 따로 뺀다 + 알고리즘 강화."
> → 엔진 v2.1.0 강화 4건 추가 완료(아래 '2차 강화'). 디자인 전면 교체 전제라면 기존 PR #1~#6 대기열은
> **새 디자인+새 엔진 PR로 대체(supersede)하는 게 자연스러움** — 아래 결정 1 참고.

## 뭘 고쳤나 (3줄)
1. **진단 엔진을 `engine/bfm-skin-engine.js` 단일 모듈로 분리** — 연재가 디자인을 통째로 갈아엎어도 이 파일만 import하면 진단 그대로. 사용법은 `engine/README.md`, 레퍼런스 화면은 `diagnose-bw.html`(엔진 소비 + 레거시 face 코드 삭제).
2. **측정 3개가 사실상 깨져 있던 걸 수정** — 광택(무광 피부도 유분 만점으로 읽힘), 붉은기(감마 변화에 붕괴), 모공(항상 0). + 사진가중 q=0.5 절벽 제거. 합성 이미지 회귀 하니스(`test/harness/`)로 수정 전후 검증.
3. **촬영 가이드 능동화** — BlazeFace 눈·코·귀 키포인트로 가이드 SVG가 사람 얼굴 위치·크기를 따라 움직이고, 얼굴을 놓치면 부드럽게 기본 위치로 복귀 (`diagnose-bw.html`의 `updateGuideTransform`).

## 지표 변화 (숫자)
| | before | after |
|---|---|---|
| 건성 사진 + 중립 설문 | **B지성 (오판)** | **A건성** |
| 트러블 사진 + 중립 설문 | **B지성 (오판)** | **C트러블** |
| 7프로필×13촬영변형 판정 일치율 | 0.960 (일관되게 틀림) | 0.967 (맞으면서 일관) |
| 붉은기 지수 흔들림(트러블 프로필) | 49/100 | 22/100 |
| 모공 측정 | 항상 0 (무의미) | 프로필 간 단조 구분 |

수치 원본: `test/baseline/before-fix.json` / `after-final.json`. 재실행: `node test/harness/run-baseline.mjs 라벨`

## 2차 강화 (엔진 v2.1.0, 같은 브랜치)
1. **점상 트러블 vs 넓은 홍조 구분** — 홍조를 여드름으로 오판하던 것 분리, '홍조 주의' 플래그 신설 (검증: flushed 프로필 C오판 → A + 플래그)
2. **촬영 순간 키포인트 ROI + 눈·입술 제외** — 입술이 프레임에 들면 트러블 축 +14점 오염되던 것 6배 감소
3. **연사 3장 중앙값** — 촬영 버튼 1번에 110ms 간격 3장, 노이즈 편차 31% 감소
4. **축별 사진 신뢰도** — 사진 가중을 유분1 > 트러블0.95 > 건조0.85로 차등(건조는 사진이 간접 추론이라 설문 우위)
수치: `test/baseline/strengthen-v2.json` (섹션 4a·4b·4c)

## 결정이 필요한 것
1. **라이브 전환 경로** — 디자인 전면 교체가 확정이면:
   - (a) 새 디자인 페이지를 처음부터 엔진 import로 작성(연재에게 `engine/README.md` 전달) → 기존 PR #1~#6은 머지 없이 닫음(내용은 엔진에 이미 포함·개선됨) ← **추천(가장 깔끔)**
   - (b) 기존 계획대로 PR #1~#6 먼저 머지 → 디자인 PR에서 엔진 전환 (안전하지만 이중 작업)
2. **모공 지수를 결과 화면에 계속 보여줄까** — 640px 셀피에서 모공 2~3px라 물리적으로 가장 약한 지표. 실기기 재보정 전까지 숨기거나 '참고' 배지 옵션 (FINDINGS R2).
3. **트러블 경계** — 설문 전수 스윕에서 '가끔 한두 개' 응답만으로도 C가 되는 경계 밀집 확인(FINDINGS R3). troubleMin(0.28)은 A/B/C 목표 배분 사업 결정이라 안 건드림 — 실물 도착 후 실기기 합의 세션에서 체감 확인 권장.
4. **이 브랜치 자체** — batch8-accuracy를 preview에 머지해 포크 Pages 프리뷰(diagnose-bw.html)에서 폰으로 체감 확인할지. 능동형 가이드는 실카메라에서만 보이므로 폰 확인 필수.

## 손대지 않은 것과 이유
- **diagnose.html(라이브 대상)** — PR #1~#6 대기열과 충돌 나므로. 결정 1번에 묶임.
- **troubleMin 등 CAL 판정 임계값** — 사업 배분 결정(대표 몫). 측정이 바뀌어도 임계값 체계는 유지.
- **업로드 경로 얼굴 검증 추가**(FINDINGS R4) — 모델 로드 1~2초 비용 vs 편익을 실기기에서 판단할 문제.
- **설문 문항·배점** — 트러블 표현력은 batch5에서 이미 손댔고, 실사용 분포 없이 더 만지는 건 과보정 위험.
- 합성 스윕은 실사진 대체가 아님 — **ROLLOUT 3단계의 실기기 9회 측정(3조명×3회)은 여전히 필요**. 이제 `?bfmdebug` + 하니스가 있어서 그 데이터로 CAL 재보정하기 쉬워짐.
