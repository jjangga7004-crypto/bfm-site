# NEXT — 아침에 읽을 요약 (최신: 2026-09-02 저녁, 엔진 v2.4.0 — **배분 확정 + 새 레포 분리**)

## 대표 결정 반영 (9/2 저녁)
1. **A/B/C 목표 배분 = 20/50/30 확정** → 문헌 프라이어 기반 모의 사용자 20,000명(population.mjs)으로
   troubleMin 0.28→**0.27**, abTilt(신규) **0.12** 탐색·적용 → 모의 분포 **A 24.3 / B 45.4 / C 30.4**
   - abTilt는 조건부: "애매하면 B" — 사진이 건조를 강하게 말하면(Sp건조≥0.7) 기울임 무효(홍조·명백 건성 보호)
   - 0.16이면 A 23.5까지 가지만 재촬영 일치가 2.8pp 나빠져 0.12 채택(일관성 우선 원칙)
   - A가 20까지 안 내려가는 건 모의 집단 자체가 건성계 25%(문헌)라서 — **최종 배분은 실기기 런로그 → `calibrate.mjs --target-c 0.3`으로 재조정**
   - 재촬영 안전망: 히스테리시스 마진 0.03→0.05 확대(경계 이동 보상), 뒤집힘율 prev 전달 시 4.3%
2. **새 레포 분리** — 엔진·하니스·레퍼런스 페이지를 대표 소유 새 저장소로 이전(아래 참조). 앞으로 기능 진화는 새 레포에서.



## 오늘(9/2) 뭘 했나 (3줄)
1. **재촬영 일관성 전용 지표 신설 후 히스테리시스 적용** — "아무 두 번 찍기" 쌍별 타입 뒤집힘율 **5.09%→2.18%**(-57%). 경계 마진(<0.03) 안에서만 직전 타입(2시간 이내 같은 기기)을 유지, 명확한 신호는 절대 안 덮음. 판정 매트릭스·설문 스윕은 바이트 단위 동일 확인
2. **업로드 경로 얼굴 검증(R4)** — 업로드 사진도 얼굴이 잡히면 키포인트 ROI·눈/입술 제외 적용(2.5초 내 못 잡으면 기존 동작, 차단 없음). + 모공 지수 '(참고치)' 표기(R2 보수판) + 강한 조명색 소프트 안내(R5, 차단 아님)
3. **검증**: 하니스 전 섹션 통과 + 브라우저에서 경계 사진으로 prev 유무·2시간 만료까지 실측 확인(oily/dry/oily 기대값 일치)

## 오늘(9/2) 지표
| 지표 | before | after |
|---|---|---|
| 재촬영 쌍별 뒤집힘율(5,460쌍) | 5.09% | **2.18%** |
| 재촬영쌍 일치 평균(신규 지표) | 0.949 | 0.949(측정 기준선) |
| 타입 매트릭스·설문 분포 | — | 완전 동일(회귀 없음) |

수치: `test/baseline/retake-baseline.json` / `retake-hyst.json`. 최악 프로필은 여전히 normal(0.851 — A/B 경계에 앉은 피부라 구조적, 히스테리시스가 실사용에서 완화).

## PR 상태 (9/2 확인)
- origin/main 변화 없음(bd5e408). **PR #1~#6 전부 아직 OPEN** — 이 브랜치(batch8)는 preview 기반이라 충돌 없음. 디자인 전면 교체 확정 시 결정 1(a)대로 PR들을 닫는 선택지 유효.

---

# (이전 요약: 2026-09-01 자율 세션 + 알고리즘 강화)

> 대표 지시(같은 날 저녁): "라이브 디자인을 모조리 바꿀 것이므로 그 전에 엔진을 따로 뺀다 + 알고리즘 강화."
> → 엔진 v2.1.0 강화 4건 추가 완료(아래 '2차 강화'). 디자인 전면 교체 전제라면 기존 PR #1~#6 대기열은
> **새 디자인+새 엔진 PR로 대체(supersede)하는 게 자연스러움** — 아래 결정 1 참고.

## 뭘 고쳤나 (3줄)
1. **진단 엔진을 `engine/bfm-skin-engine.js` 단일 모듈로 분리** — 연재가 디자인을 통째로 갈아엎어도 이 파일만 import하면 진단 그대로. 사용법은 `engine/README.md`, 레퍼런스 화면은 `diagnose-bw.html`(엔진 소비 + 레거시 face 코드 삭제).
2. **측정 3개가 사실상 깨져 있던 걸 수정** — 광택(무광 피부도 유분 만점으로 읽힘), 붉은기(감마 변화에 붕괴), 모공(항상 0). + 사진가중 q=0.5 절벽 제거. 합성 이미지 회귀 하니스(`test/harness/`)로 수정 전후 검증.
3. **촬영 가이드 능동화** — BlazeFace 눈·코·귀 키포인트로 가이드 SVG가 사람 얼굴 위치·크기를 따라 움직이고, 얼굴을 놓치면 부드럽게 기본 위치로 복귀 (`diagnose-bw.html`의 `updateGuideTransform`).

## 지표 변화 (숫자)
| | before | after |
|---|---|---|
| 건성 사진 + 중립 설문 | **B지성 (오판)** | **A건성** |
| 트러블 사진 + 중립 설문 | **B지성 (오판)** | **C트러블** |
| 7프로필×13촬영변형 판정 일치율 | 0.960 (일관되게 틀림) | 0.967 (맞으면서 일관) |
| 붉은기 지수 흔들림(트러블 프로필) | 49/100 | 22/100 |
| 모공 측정 | 항상 0 (무의미) | 프로필 간 단조 구분 |

수치 원본: `test/baseline/before-fix.json` / `after-final.json`. 재실행: `node test/harness/run-baseline.mjs 라벨`

## 2차 강화 (엔진 v2.1.0, 같은 브랜치)
1. **점상 트러블 vs 넓은 홍조 구분** — 홍조를 여드름으로 오판하던 것 분리, '홍조 주의' 플래그 신설 (검증: flushed 프로필 C오판 → A + 플래그)
2. **촬영 순간 키포인트 ROI + 눈·입술 제외** — 입술이 프레임에 들면 트러블 축 +14점 오염되던 것 6배 감소
3. **연사 3장 중앙값** — 촬영 버튼 1번에 110ms 간격 3장, 노이즈 편차 31% 감소
4. **축별 사진 신뢰도** — 사진 가중을 유분1 > 트러블0.95 > 건조0.85로 차등(건조는 사진이 간접 추론이라 설문 우위)
수치: `test/baseline/strengthen-v2.json` (섹션 4a·4b·4c)

## 3차 — 기능 진화 체계 (엔진 v2.2.0, "기능은 계속 진화" 지시 반영)
1. **건조 축 직접 신호(각질·거칠기)** — 지금까지 건조는 '광택 없음' 추론뿐이었음. 각질 지수 신설(veryDry 86/dry 60/normal 10), 근거 문구·부위 지수에 연결
2. **측정 런로그** — 진단 1회마다 측정값(숫자만, 사진 없음)을 기기 로컬에 기록, ?bfmdebug 결과 화면에서 [복사]로 내보내기
3. **calibrate.mjs** — 내보낸 실측 로그로 CAL 재보정안 자동 제안 + **troubleMin↔C비율 표**(A/B/C 배분을 숫자 보고 정할 수 있음, `--target-c 0.3` 지원)
4. **engine/ROADMAP.md** — v2.3(실측 재보정)→v2.4(업로드 얼굴검증)→v2.5(세션 간 일관성)→v3.0(데이터 플라이휠)→v3.1(AI 정밀분석 옵트인) 단계별 게이트 명시

## 4차 — 진단 없이 한 "학습" (문헌 + 실사진, v2.2.1)
1. **문헌 앵커링** — 논문 조사 3건으로 상수·설계를 근거에 연결(`engine/SCIENCE.md`). 병변 <5mm 기준으로 점상 블롭 경계 재설정 → 일관성 0.963→**0.972**. "볼=기준부위" 설계, 점상/홍조 이분법이 임상 표준과 일치함을 확인
2. **실사진 검증** — 공개 인물사진 8장에 엔진을 돌려(사진은 로컬 처리만) 홍조·각질 측정의 실사진 오탐 2건 발견·수정(코 그림자→홍조 오탐, 눈썹→각질 오탐 — 둘 다 볼 기반으로 변경)
3. **프라이어 참고치 확보** — 20대 한국 남성 지성 57%(Baumann n=1,000), 26~29세는 건성계 최다. troubleMin 결정 시 calibrate.mjs 표와 함께 쓸 것
4. **측정 설계 주의사항** — 일중 피지 변동 ±30%(피크 13:20): 실기기 9회 측정은 시간대 통제 필요

## 결정이 필요한 것 (9/2에도 유효 — 아래는 진행 안 함)
1. **라이브 전환 경로** — 디자인 전면 교체가 확정이면:
   - (a) 새 디자인 페이지를 처음부터 엔진 import로 작성(연재에게 `engine/README.md` 전달) → 기존 PR #1~#6은 머지 없이 닫음(내용은 엔진에 이미 포함·개선됨) ← **추천(가장 깔끔)**
   - (b) 기존 계획대로 PR #1~#6 먼저 머지 → 디자인 PR에서 엔진 전환 (안전하지만 이중 작업)
2. ~~모공 지수~~ → 9/2에 보수판 적용: 라벨을 '(참고치)'로 변경(숨김 여부는 여전히 대표 몫).
3. **트러블 경계** — 설문 전수 스윕에서 '가끔 한두 개' 응답만으로도 C가 되는 경계 밀집 확인(FINDINGS R3). troubleMin(0.28)은 A/B/C 목표 배분 사업 결정이라 안 건드림 — 실물 도착 후 실기기 합의 세션에서 체감 확인 권장.
4. **이 브랜치 자체** — batch8-accuracy를 preview에 머지해 포크 Pages 프리뷰(diagnose-bw.html)에서 폰으로 체감 확인할지. 능동형 가이드는 실카메라에서만 보이므로 폰 확인 필수.

## 손대지 않은 것과 이유
- **diagnose.html(라이브 대상)** — PR #1~#6 대기열과 충돌 나므로. 결정 1번에 묶임.
- **troubleMin 등 CAL 판정 임계값** — 사업 배분 결정(대표 몫). 측정이 바뀌어도 임계값 체계는 유지.
- **업로드 경로 얼굴 검증 추가**(FINDINGS R4) — 모델 로드 1~2초 비용 vs 편익을 실기기에서 판단할 문제.
- **설문 문항·배점** — 트러블 표현력은 batch5에서 이미 손댔고, 실사용 분포 없이 더 만지는 건 과보정 위험.
- 합성 스윕은 실사진 대체가 아님 — **ROLLOUT 3단계의 실기기 9회 측정(3조명×3회)은 여전히 필요**. 이제 `?bfmdebug` + 하니스가 있어서 그 데이터로 CAL 재보정하기 쉬워짐.
